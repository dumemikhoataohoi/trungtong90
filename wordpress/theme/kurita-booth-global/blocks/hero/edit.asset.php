<?php
/**
 * Hand-written dependency manifest (no build step / no @wordpress/scripts).
 * Same list reused by every block in this theme — see blocks/_shared-deps.php.
 */
return require dirname( __DIR__ ) . '/_shared-deps.php';
